	// Print all primes up to a given number

	var ask = require('readline-sync');

	function isPrimeAndPrint (prime)
	{
		for(i = 0; i<prime ; i++)
		{

				if(i % prime == 0)
				{

				}
		}
	}

	console.log('Choose a number to print all primes to: ');
